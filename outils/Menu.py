print("Je fonctionne")



'''
def main():
    # On vérifie qu'un fichier ROM a été passé en argument
    if len(sys.argv) < 2:
        print("Usage: python emulator.py <fichier.ch8>")
        sys.exit(1)

    rompath = sys.argv[1]
    print(f"Exécution de la ROM : {rompath}")

    # On crée l'instance du CPU et on lance l'émulation
    cpu = chip8()
    cpu.run(rompath)


if __name__ == '__main__':
    main()
'''